---
- name: Generar archivo en la máquina rhel
hosts: localhost
tasks:

- name: crear archivo
  copy:
    dest: “{{ ruta_archivo }}/{{ item }}.txt”
    content: |
      “{{ list_info }}”
  with_items: “{{ machine_names }}”
