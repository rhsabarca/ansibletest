---
- name: conexion maquinas windows
  hosts: all
  tasks:
    - name: windows ping
      win_ping:
      register: registro_ping

    - name: print resultados
      ansible.built.debug:
        msg: "{{ registro_ping }} "
